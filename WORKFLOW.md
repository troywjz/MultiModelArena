# 工作流

## 开发循环

1. 明确本次任务的验收结果。
2. 阅读相关文档和代码。
3. 小范围修改。
4. 运行相关验证。
5. 如产生可复用知识，更新文档、测试或脚本。
6. 汇报变更、验证和剩余风险。

## 分支与提交

当前项目尚未初始化 Git。初始化后建议：

- `main` 保持可运行。
- 功能按小分支开发。
- 提交信息使用中文，说明用户可见行为或工程边界。

## 配置与密钥

- 提交 `.env.example`，不提交 `.env`。
- 任何日志、报告和测试快照都不能包含真实 API Key。
- 新增供应商适配器时，需要说明请求格式、错误格式、超时和重试策略。

## 发布与运行

MVP 发布目标是本地命令行运行：

```powershell
python -m arena run
python -m arena report --input runs/latest
```

后续可增加：

- `python -m arena serve` 启动本地网页服务。
- Docker 镜像。
- GitHub Actions 运行单元测试和格式检查。
